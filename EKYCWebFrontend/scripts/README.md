This folder contains utility scripts used during install/build to verify dependency alignment required by CRA5/webpack5.
- verify-toolchain.js: Ensures schema-utils v3 is installed to prevent "validate is not a function" errors at build time.
